import type { AppMeta } from "../../shell/appRegistry";
export const appMeta: AppMeta = Object.freeze({ id: 'arcade', title: 'Arcade.EXE', icon: 'arcade' });

import type { AppMeta } from "../../shell/appRegistry";
export const appMeta: AppMeta = Object.freeze({
  id: "arcade",
  title: "Arcade.EXE",
  icon: "arcade",
});
