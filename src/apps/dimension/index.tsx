/**
 * SUMMARY
 * Dimension app stub. Provides minimal component and metadata for the registry.
 */
import React from 'react';

export const appMeta = { id: 'dimension', title: 'Dimension.EXE', icon: 'dimension' };

export default function DimensionApp() {
    return (
        <div>
            <h1>Dimension.EXE</h1>
            <p>Stub app. 3D playground → wallpaper → shared space.</p>
        </div>
    );
}


